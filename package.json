{
  "name": "bounce-court",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Bounce Court — a physics volleyball arcade game for the browser",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "three": "^0.169.0",
    "@types/three": "^0.169.0"
  }
}
